<head>
  <style>
    
    @media (max-width: 600px) {
      .pharmacy-cards {
        grid-template-columns: 1fr;
        gap: 24px;
        padding: 8px 4px;
      }

      .pharmacy-card {
        padding: 28px 20px 20px;
        margin-bottom: 15px;
      }

      .pharmacy-card.featured-pharmacy {
        margin-top: 16px;
        overflow: visible;
      }

      .pharmacy-card.featured-pharmacy .holo-sheen,
      .pharmacy-card.featured-pharmacy .ripple {
        overflow: hidden;
        border-radius: 10px;
      }

      .pharmacy-card .price {
        font-size: 26px;
      }
    }

    .form-group {
      margin-bottom: 20px;
    }

    .search {
      margin-bottom: 20px;
      background: #3884a4;
      color: white;
    }

    .search:hover {
      background-color: #006868;
    }

    .form-group label {
      display: block;
      margin-bottom: 8px;
      font-weight: normal;
      color: #989898;
    }

    .form-group input,
    .form-group select,
    .form-group button {
      width: 100%;
      height: 60px;
      padding: 10px;
      font-size: 16px;
      border: 1px solid #ccc;
      border-radius: 14px;
    }

    .suggestions {
      border-radius: 4px;
      max-height: 200px;
      overflow-y: auto;
      background: white;
      z-index: 0;
      width: 100%;
    }

    .drug-option {
      padding: 10px;
      cursor: pointer;
    }

    .drug-option:hover {
      background-color: #f0f0f0;
    }

    .recent-searches {
      margin-top: 20px;
      padding: 10px;
      border-radius: 16px;
    }

    .recent-searches .search-item {
      padding: 8px 12px;
      margin: 5px 0;
      background-color: #44908db3;
      color: white;
      border-radius: 16px;
      cursor: pointer;
      display: flex;
      align-items: center;
    }

    .recent-searches .search-item:hover {
      background-color: #44908db3;
    }

    .recent-searches .search-item.active-search {
      background-color: #35858b;
    }

    .recent-searches .search-item.active-search .clear-search {
      z-index: 10;
    }

    #errorMessage {
      color: red;
      text-align: center;
    }

    .pharmacy-cards {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 20px;
      margin-top: 30px;
    }

    .pharmacy-card {
      background: white;
      text-align: center;
      padding: 24px 16px 20px;
      border-radius: 12px;
      border: 2px solid transparent;
      box-shadow: 0 2px 12px rgba(0,0,0,0.07);
      cursor: pointer;
      position: relative;
      transform-origin: center center;
      will-change: transform;
      overflow: hidden;
      transform-style: preserve-3d;
      transition:
        transform 0.65s cubic-bezier(0.34, 1.42, 0.64, 1),
        background-color 0.4s ease,
        border-color 0.4s ease,
        box-shadow 0.45s cubic-bezier(0.34, 1.42, 0.64, 1);
    }

    .pharmacy-card:hover:not(.selected):not(.animate-click) {
      transform: scale(1.03);
      box-shadow: 0 8px 24px rgba(0,0,0,0.1);
    }

    .pharmacy-card h3 {
      margin-bottom: 10px;
      font-size: 18px;
    }

    .pharmacy-card p {
      margin: 5px 0;
    }

    .pharmacy-card .price {
      font-size: 32px;
      color: black;
      font-weight: bold;
      transition: color 0.4s ease, transform 0.4s ease;
      position: relative;
      z-index: 1;
    }

    .pharmacy-card.selected {
      background-color: #eef4fb;
      border-color: #3884a4;
      transform: scale(1.075) !important;
      box-shadow: 0 22px 50px rgba(56,132,164,0.22) !important;
    }

    .pharmacy-card.selected .price {
      color: #3884a4;
      transform: scale(1.06);
    }

    .pharmacy-card.featured-pharmacy {
      border-color: #44908d;
      background-color: #f5f5f5;
      overflow: visible;
    }

    .pharmacy-card.featured-pharmacy.selected {
      background-color: #fdf8ee;
      border-color: #c9a84c;
      transform: scale(1.075) !important;
      box-shadow: 0 22px 50px rgba(180,140,60,0.2) !important;
    }

    .pharmacy-card.featured-pharmacy.selected .price {
      color: #a07830;
    }

    .pharmacy-card.animate-click {
      animation: punchBounce 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
    }

    @keyframes punchBounce {
      0%   { transform: scale(1.03);  }
      30%  { transform: scale(1.14);  }
      100% { transform: scale(1.075); }
    }

    .pharmacy-card.animate-deselect {
      animation: punchDeselect 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
    }

    @keyframes punchDeselect {
      0%   { transform: scale(1.075); }
      40%  { transform: scale(0.97);  }
      100% { transform: scale(1);     }
    }

    .holo-sheen {
      position: absolute;
      inset: 0;
      border-radius: 10px;
      pointer-events: none;
      z-index: 2;
      opacity: 0;
      transition: opacity 0.35s ease;
    }

    .featured-badge {
      position: absolute;
      top: -13px;
      left: 50%;
      transform: translateX(-50%);
      background: #44908d;
      color: white;
      font-size: 10px;
      font-weight: 600;
      padding: 3px 12px;
      border-radius: 20px;
      letter-spacing: 0.8px;
      text-transform: uppercase;
      white-space: nowrap;
      overflow: hidden;
      transition:
        transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1),
        box-shadow 0.4s ease,
        background-color 0.4s ease;
      z-index: 3;
    }

    .pharmacy-card.featured-pharmacy.selected .featured-badge {
      background: #c9a84c;
      transform: translateX(-50%) scale(1.07);
      box-shadow: 0 3px 10px rgba(180,140,60,0.35);
    }

    .featured-badge .shimmer-bar {
      position: absolute;
      top: 0; left: -60%;
      width: 40%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.6), transparent);
      animation: shimmerLoop 4s ease-in-out 1s infinite;
    }

    @keyframes shimmerLoop {
      0%   { left: -60%; opacity: 0; }
      5%   { opacity: 1; }
      40%  { left: 160%; opacity: 1; }
      41%  { opacity: 0; }
      100% { left: 160%; opacity: 0; }
    }

    /* Dot — regular cards only */
    .dot {
      position: absolute;
      bottom: 10px;
      right: 12px;
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: #3884a4;
      opacity: 0;
      transform: scale(0);
      transition: opacity 0.15s ease;
      z-index: 3;
    }

    .pharmacy-card.selected .dot {
      opacity: 1;
      animation: dotPop 0.5s cubic-bezier(0.34, 1.9, 0.64, 1) forwards,
                 dotPulse 2s ease-in-out 0.55s infinite;
    }

    /* Featured cards use star instead — hide dot entirely */
    .pharmacy-card.featured-pharmacy .dot {
      display: none;
    }

    @keyframes dotPop {
      0%   { transform: scale(0);    }
      55%  { transform: scale(1.5);  }
      75%  { transform: scale(0.82); }
      100% { transform: scale(1);    }
    }

    @keyframes dotPulse {
      0%,100% { box-shadow: 0 0 0 0px rgba(56,132,164,0.45); }
      50%     { box-shadow: 0 0 0 5px rgba(56,132,164,0);    }
    }

    /* Star canvas — featured cards only */
    .star-canvas {
      position: absolute;
      bottom: 4px;
      right: 4px;
      width: 28px;
      height: 28px;
      z-index: 10;
      opacity: 0;
      pointer-events: none;
    }

    .pharmacy-card.featured-pharmacy.selected .star-canvas {
      animation: starPop 0.7s cubic-bezier(0.34, 2.0, 0.64, 1) 0s forwards;
    }

    @keyframes starPop {
      0%   { opacity: 0; transform: scale(0) rotate(-45deg); }
      55%  { opacity: 1; transform: scale(1.5) rotate(8deg); }
      75%  { transform: scale(0.9) rotate(-2deg); }
      100% { opacity: 1; transform: scale(1) rotate(0deg); }
    }

    /* Ripple */
    .ripple {
      position: absolute;
      border-radius: 50%;
      background: rgba(56,132,164,0.15);
      transform: scale(0);
      pointer-events: none;
      animation: rippleOut 0.55s ease-out forwards;
      z-index: 0;
      overflow: visible;
    }

    .featured-pharmacy .ripple { background: rgba(180,140,60,0.15); }

    @keyframes rippleOut {
      to { transform: scale(4.5); opacity: 0; }
    }

    /* Staggered card entrance */
    .pharmacy-card {
      opacity: 0;
      transform: scale(0.96);
    }

    .pharmacy-card.card-visible {
      opacity: 1;
      transform: scale(1);
    }

    .loader {
      border: 8px solid #f3f3f3;
      border-top: 8px solid #3884a4;
      border-radius: 50%;
      width: 50px;
      height: 50px;
      animation: spin 1s linear infinite;
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      display: none;
    }

    .clear-search {
      color: #fff;
      padding-left: 15px;
      position: relative;
    }

    @keyframes spin {
      0%   { transform: rotate(0deg);   }
      100% { transform: rotate(360deg); }
    }

    #dynamic-loader {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.7);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 9999;
    }

    .pill-container {
      text-align: center;
      position: relative;
    }

    .capsule {
      width: 100px;
      height: 40px;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
      animation: openCapsule 2s infinite alternate ease-in-out;
    }

    .half {
      width: 50px;
      height: 40px;
      position: absolute;
      transition: transform 0.5s ease-in-out;
    }

    .half-left {
      background: #3884a4;
      border-radius: 50% 0 0 50%;
      left: 0;
    }

    .half-right {
      background: white;
      border-radius: 0 50% 50% 0;
      right: 0;
    }

    @keyframes openCapsule {
      0%   { transform: scaleX(1);   }
      100% { transform: scaleX(1.2); }
    }

    .pill-container:hover .half-left  { transform: translateX(-20px); }
    .pill-container:hover .half-right { transform: translateX(20px);  }

    .sparkles {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 80px;
      height: 50px;
      overflow: hidden;
      pointer-events: none;
    }

    .sparkle {
      width: 8px;
      height: 8px;
      background: yellow;
      border-radius: 50%;
      position: absolute;
      opacity: 0;
      animation: sparkleEffect 1.5s infinite ease-in-out;
    }

    .sparkle:nth-child(1) { left: 10px; animation-delay: 0s;   }
    .sparkle:nth-child(2) { left: 30px; animation-delay: 0.3s; }
    .sparkle:nth-child(3) { left: 50px; animation-delay: 0.6s; }
    .sparkle:nth-child(4) { left: 70px; animation-delay: 0.9s; }
    .sparkle:nth-child(5) { left: 90px; animation-delay: 1.2s; }

    @keyframes sparkleEffect {
      0%   { top: 20px;  opacity: 1; transform: scale(0.5); }
      50%  { opacity: 1; transform: scale(1.2); }
      100% { top: -20px; opacity: 0; transform: scale(0.8); }
    }

    .loading-message {
      color: white;
      font-size: 18px;
      margin-top: 15px;
      font-family: Arial, sans-serif;
    }

  </style>
</head>
